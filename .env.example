# Environment Variables Configuration

# OpenAI API Key (optional - system defaults to mock client for offline testing)
OPENAI_API_KEY=

# Anthropic API Key (optional)
ANTHROPIC_API_KEY=

# LLM Mode: "real" to use actual LLM providers, "mock" for offline testing
# Default: mock
LLM_MODE=mock

# Run ID prefix (optional - auto-generated if not set)
RUN_ID_PREFIX=

# Cache directory (optional - defaults to runs/llm_cache.db)
# CACHE_DIR=runs/

# Enable decision logging (optional - defaults to true)
# ENABLE_DECISION_LOGGING=true

# Log level (optional - defaults to INFO)
# LOG_LEVEL=INFO
